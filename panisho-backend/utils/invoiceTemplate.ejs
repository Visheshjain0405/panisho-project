<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panisho Invoice</title>
    <style>
        @page {
            size: A4;
            margin: 15mm;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.4;
            color: #333;
            font-size: 11pt;
            background: white;
        }

        .invoice-container {
            width: 100%;
            max-width: 210mm;
            margin: 0 auto;
            background: white;
            border: 1px solid #ddd;
        }

        /* Top Pink Bar */
        .top-bar {
            background: linear-gradient(135deg, #e91e63, #f8bbd9);
            height: 8px;
            width: 100%;
        }

        /* Header Section */
        .header {
            background: #f8f9fa;
            padding: 25px 30px;
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            border-bottom: 1px solid #e9ecef;
        }

        .company-section {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .logo-circle {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #e91e63, #f472b6);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            font-size: 14pt;
            box-shadow: 0 3px 10px rgba(233, 30, 99, 0.3);
        }

        .company-details {
            flex: 1;
        }

        .company-name {
            font-size: 18pt;
            font-weight: bold;
            color: #e91e63;
            margin-bottom: 3px;
            letter-spacing: 1px;
        }

        .company-address {
            font-size: 9pt;
            color: #666;
            line-height: 1.3;
        }

        .invoice-header {
            text-align: right;
        }

        .invoice-title {
            font-size: 28pt;
            font-weight: bold;
            color: #999;
            letter-spacing: 2px;
            margin-bottom: 15px;
        }

        .invoice-meta {
            text-align: right;
        }

        .meta-row {
            margin-bottom: 8px;
            font-size: 10pt;
        }

        .meta-label {
            color: #666;
            font-weight: bold;
            display: inline-block;
            width: 80px;
        }

        .meta-value {
            color: #333;
        }

        /* Payment Terms */
        .payment-terms {
            text-align: right;
            font-size: 9pt;
            color: #666;
            font-style: italic;
            margin-top: 15px;
        }

        /* Bill To Section */
        .bill-to-section {
            display: flex;
            padding: 25px 30px;
            gap: 40px;
            background: white;
        }

        .bill-to,
        .ship-to {
            flex: 1;
        }

        .section-header {
            font-size: 10pt;
            font-weight: bold;
            color: #333;
            margin-bottom: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .address-details {
            font-size: 10pt;
            line-height: 1.5;
            color: #555;
        }

        .contact-name {
            font-weight: bold;
            color: #333;
            margin-bottom: 3px;
        }

        /* Items Table */
        .items-section {
            margin: 0;
        }

        .items-table {
            width: 100%;
            border-collapse: collapse;
        }

        .items-table th {
            background: linear-gradient(135deg, #e91e63, #f472b6);
            color: white;
            padding: 12px 15px;
            text-align: left;
            font-weight: bold;
            font-size: 10pt;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .items-table th:last-child {
            text-align: right;
        }

        .items-table td {
            padding: 12px 15px;
            border-bottom: 1px solid #e9ecef;
            font-size: 10pt;
            vertical-align: top;
        }

        .items-table tr:nth-child(even) {
            background: #fafafa;
        }

        .product-name {
            font-weight: bold;
            color: #333;
            margin-bottom: 3px;
        }

        .product-desc {
            color: #666;
            font-size: 9pt;
            line-height: 1.3;
        }

        .text-right {
            text-align: right;
        }

        .text-center {
            text-align: center;
        }

        /* Summary Section */
        .summary-section {
            display: flex;
            padding: 25px 30px;
            gap: 40px;
        }

        .remarks {
            flex: 1;
        }

        .remarks-title {
            font-size: 10pt;
            font-weight: bold;
            color: #333;
            margin-bottom: 8px;
        }

        .remarks-content {
            font-size: 9pt;
            color: #666;
            line-height: 1.4;
        }

        .totals {
            flex: 0 0 300px;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 6px 0;
            font-size: 10pt;
        }

        .total-label {
            color: #666;
            text-transform: uppercase;
            font-size: 9pt;
        }

        .total-value {
            font-weight: bold;
            color: #333;
            text-align: right;
        }

        .discount-value {
            color: #28a745;
        }

        .balance-due {
            background: linear-gradient(135deg, #e91e63, #f472b6);
            color: white;
            padding: 12px 15px;
            margin-top: 10px;
            border-radius: 5px;
            display: flex;
            justify-content: space-between;
            font-weight: bold;
            font-size: 12pt;
        }

        /* Coupon Section */
        .coupon-notice {
            background: #e8f5e8;
            border-left: 4px solid #28a745;
            padding: 10px 15px;
            margin: 0 30px;
            font-size: 10pt;
            color: #155724;
        }

        /* Footer */
        .footer {
            margin-top: 30px;
        }

        .bottom-bar {
            background: linear-gradient(135deg, #e91e63, #f8bbd9);
            height: 8px;
            width: 100%;
        }

        .terms-footer {
            padding: 20px 30px;
            background: #f8f9fa;
            border-top: 1px solid #e9ecef;
        }

        .terms-title {
            font-size: 11pt;
            font-weight: bold;
            color: #e91e63;
            margin-bottom: 10px;
        }

        .terms-text {
            font-size: 9pt;
            color: #666;
            line-height: 1.4;
        }

        .no-return {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            padding: 12px;
            margin: 15px 30px;
            border-radius: 3px;
            text-align: center;
        }

        .no-return-title {
            font-weight: bold;
            color: #856404;
            font-size: 10pt;
            margin-bottom: 5px;
        }

        .no-return-text {
            font-size: 9pt;
            color: #856404;
        }

        .contact-footer {
            text-align: center;
            padding: 15px;
            font-size: 9pt;
            color: #666;
            background: #f8f9fa;
        }

        /* Print Styles */
        @media print {
            body {
                font-size: 10pt;
            }

            .invoice-container {
                border: none;
            }

            .items-table {
                page-break-inside: avoid;
            }

            .summary-section {
                page-break-inside: avoid;
            }

            .terms-footer {
                page-break-inside: avoid;
            }
        }
    </style>
</head>

<body>
    <div class="invoice-container">
        <div class="top-bar"></div>

        <div class="header">
            <div class="company-section">
                <div class="logo-circle">P</div>
                <div class="company-details">
                    <div class="company-name">PANISHO</div>
                    <div class="company-address">
                        Premium Beauty & Wellness Products<br>
                        Navsari, Gujarat, India<br>
                        support@panisho.com | +91 9054353157
                    </div>
                </div>
            </div>
            <div class="invoice-header">
                <div class="invoice-title">INVOICE</div>
                <div class="invoice-meta">
                    <div class="meta-row">
                        <span class="meta-label">DATE:</span>
                        <span class="meta-value">
                            <%= new Date(order.createdAt).toLocaleDateString('en-GB') %>
                        </span>
                    </div>
                    <div class="meta-row">
                        <span class="meta-label">INVOICE NO:</span>
                        <span class="meta-value">INV-<%= order._id.toString().slice(-8).toUpperCase() %></span>
                    </div>
                </div>
                <div class="payment-terms">
                    Payment terms: <%= order.paymentMethod==='cod' ? 'Cash on Delivery' : 'Online Payment' %>
                </div>
            </div>
        </div>

        <div class="bill-to-section">
            <div class="bill-to">
                <div class="section-header">Bill To</div>
                <div class="address-details">
                    <div class="contact-name">
                        <%= address.name || user.firstName + ' ' + user.lastName %>
                    </div>
                    <div>
                        <%= address.street %>
                    </div>
                    <div>
                        <%= address.city %>
                    </div>
                    <div>
                        <%= address.state %>
                            <%= address.pincode %>
                    </div>
                    <div>
                        <%= address.phone || user.mobile %>
                    </div>
                </div>
            </div>
            <div class="ship-to">
                <div class="section-header">Ship To</div>
                <div class="address-details">
                    <div class="contact-name">
                        <%= address.name || user.firstName + ' ' + user.lastName %>
                    </div>
                    <div>
                        <%= address.street %>
                    </div>
                    <div>
                        <%= address.city %>
                    </div>
                    <div>
                        <%= address.state %>
                            <%= address.pincode %>
                    </div>
                    <div>
                        <%= address.phone || user.mobile %>
                    </div>
                </div>
            </div>
        </div>

        <% if (order.coupon?.code) { %>
            <div class="coupon-notice">
                <strong>✓ Discount Applied:</strong> Coupon Code "<%= order.coupon.code %>" - You saved ₹<%=
                        order.discount.toFixed(2) %>
            </div>
            <% } %>

                <div class="items-section">
                    <table class="items-table">
                        <thead>
                            <tr>
                                <th style="width: 50%;">Description</th>
                                <th style="width: 10%;" class="text-center">Qty</th>
                                <th style="width: 20%;" class="text-right">Unit Price</th>
                                <th style="width: 20%;" class="text-right">Total</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% items.forEach(item=> { %>
                                <tr>
                                    <td>
                                        <div class="product-name">
                                            <%= item.product.name %> (<%= item.variant.volume %>
                                                    <%= item.variant.volumeUnit %>)
                                        </div>
                                        <div class="product-desc">
                                            <%= item.product.description || '—' %>
                                        </div>
                                    </td>
                                    <td class="text-center">
                                        <%= item.quantity %>
                                    </td>
                                    <td class="text-right">₹<%= item.variant.sellingPrice.toFixed(2) %>
                                    </td>
                                    <td class="text-right">₹<%= (item.variant.sellingPrice * item.quantity).toFixed(2)
                                            %>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                </div>

                <div class="summary-section">
                    <div class="remarks">
                        <div class="remarks-title">Remarks / Payment Instructions:</div>
                        <div class="remarks-content">
                            Thank you for choosing Panisho! Please have the exact amount ready for <%=
                                order.paymentMethod==='cod' ? 'Cash on Delivery' : 'Online Payment' %>.
                                For any queries, contact us at support@panisho.com or +91 9054353157.
                        </div>
                    </div>
                    <div class="totals">
                        <div class="total-row">
                            <span class="total-label">Subtotal:</span>
                            <span class="total-value">₹<%= order.subtotal.toFixed(2) %></span>
                        </div>
                        <% if (order.discount> 0) { %>
                            <div class="total-row">
                                <span class="total-label">Discount:</span>
                                <span class="total-value discount-value">-₹<%= order.discount.toFixed(2) %></span>
                            </div>
                            <div class="total-row">
                                <span class="total-label">Subtotal Less Discount:</span>
                                <span class="total-value">₹<%= (order.subtotal - order.discount).toFixed(2) %></span>
                            </div>
                            <% } %>
                                <div class="total-row">
                                    <span class="total-label">Tax Rate:</span>
                                    <span class="total-value">0.00%</span>
                                </div>
                                <div class="total-row">
                                    <span class="total-label">Total Tax:</span>
                                    <span class="total-value">₹0.00</span>
                                </div>
                                <div class="total-row">
                                    <span class="total-label">Shipping/Handling:</span>
                                    <span class="total-value">₹<%= order.shipping.toFixed(2) %></span>
                                </div>
                                <div class="balance-due">
                                    <span>Balance Due</span>
                                    <span>₹<%= order.total.toFixed(2) %></span>
                                </div>
                    </div>
                </div>

                <div class="no-return">
                    <div class="no-return-title">🚫 NO RETURN POLICY</div>
                    <div class="no-return-text">
                        Due to hygiene reasons, beauty and wellness products cannot be returned.
                        For quality issues, contact us within 24 hours of delivery.
                    </div>
                </div>

                <div class="footer">
                    <div class="terms-footer">
                        <div class="terms-title">Terms & Conditions</div>
                        <div class="terms-text">
                            1. Payment due on delivery for COD orders. 2. Delivery takes 3-7 business days.
                            3. Ensure availability at delivery address. 4. Use products as per instructions.
                            5. Store in cool, dry place. 6. Contact customer service for queries.
                            7. Computer-generated invoice, no signature required. 8. Prices in INR, inclusive of
                            applicable taxes.
                        </div>
                    </div>
                    <div class="contact-footer">
                        <strong>PANISHO</strong> | support@panisho.com | +91 9054353157 | www.panisho.com<br>
                        Business Hours: Monday to Saturday, 9:00 AM - 6:00 PM
                    </div>
                    <div class="bottom-bar"></div>
                </div>
    </div>
</body>

</html>